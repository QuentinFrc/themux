# Required Neon connection string
NEON_DATABASE_URL="postgresql://user:password@host/db"

# Optional: fallback database URL if you want to keep compatibility
# DATABASE_URL="postgresql://user:password@host/db"
